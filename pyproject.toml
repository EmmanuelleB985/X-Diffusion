[tool.pytest.ini_options]
pythonpath = [".", "scripts"]
testpaths = ["scripts/tests"]
python_files = ["test_*.py", "tests_*.py"]
markers = [
    "slow: slow tests",
]

[tool.black]
target-version = ["py39"]

[tool.isort]
profile = "black"
